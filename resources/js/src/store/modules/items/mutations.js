export const SET_PICKED_CHAMPION = (state, championName) =>
    (state.pickedChampionName = championName);

// export const SET_PICKED_CHAMPION = (state, data) => {
//     state.pickedChampionName = data;
// };
