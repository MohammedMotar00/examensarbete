export const SET_LOGIN = (state, username) => {
    if (username) state.username = username;
};

export const SET_USERNAME_LOGGED_IN = (state, username) => {
    if (username) state.username = username;
};
